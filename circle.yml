dependencies:
  pre:
    - npm install -g npm
machine:
  node:
    version: 4.4.7
  services:
    - postgresql
database:
    override:
    - createdb testing
general:
  artifacts:
    - "coverage"
test:
    post:
        - bash <(curl -s https://codecov.io/bash)
