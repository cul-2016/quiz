import React, { PropTypes } from 'react'; //eslint-disable-line no-unused-vars

const QuizHistory = () => {

    return (
        <div>
            This is the quiz history
        </div>
    );
};

export default QuizHistory;
