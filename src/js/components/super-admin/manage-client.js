import React from 'react';

const SuperAdminManageClient = ({ name, email, accountType, paid, code }) => {

    return (
        <div>
          Helloooooo
            { name }
            { paid }
        </div>
    )
};

export default SuperAdminManageClient;