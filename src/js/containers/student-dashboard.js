import { connect } from 'react-redux';
import StudentDashboard from '../components/student/dashboard';


const StudentDashboardContainer = connect()(StudentDashboard);

export default StudentDashboardContainer;
